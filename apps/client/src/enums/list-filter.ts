export enum ListFilterButtonVariant {
  DEFAULT = 'DEFAULT',
  LIGHT = 'LIGHT',
  BRAND = 'BRAND',
}

export enum ListFilterButtonSearchState {
  ALL = 'ALL',
  BY_CATEGORY = 'BY_CATEGORY',
}

export enum ListFilterButtonSortState {
  DEFAULT = 'DEFAULT',
  DESC = 'DESC',
  ASC = 'ASC',
}
